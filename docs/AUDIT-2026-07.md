# nflreadts Comprehensive Audit — July 2026

Audit of `@nflverse/nflreadts` v0.3.0 against nflreadr **1.5.1** (CRAN, 2026-04-13) and nflreadpy **0.1.5** (2025-11-19), plus a live smoke test of every loader against current nflverse-data release assets (2026-07-02).

## 1. Local health (good)

- 800/800 unit tests pass; typecheck and lint clean; build clean (66KB ESM / 68KB CJS).
- Branch `work-for-v0.4.0`, clean tree.
- Published: `@nflverse/nflreadts` 0.3.0. The old unscoped `nflreadts@0.1.1` should be npm-deprecated with a pointer to the scoped package.
- `@nflverse/types` was **never published** (npm 404). Local repo `nflverse-types` is canonical; `nflverse-types-package` is a non-git duplicate whose `tests/` should be absorbed, then the folder deleted.

## 2. Live smoke test (the real story)

All unit tests mock HTTP, so CI cannot detect nflverse asset drift. Live results:

| Loader              | Live result                           | Root cause                                                                                                                                                    |
| ------------------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `loadPbp`           | PASS (48,771 plays 2025, csv+parquet) | —                                                                                                                                                             |
| `loadRosters`       | PASS (3,137 rows 2025)                | —                                                                                                                                                             |
| `loadPlayers`       | PASS (25,033 rows)                    | — (but see players-v2 migration below)                                                                                                                        |
| `loadDepthCharts`   | PASS (554,215 rows 2025)              | — (but 1.5.0 restructured to date-level; verify schema)                                                                                                       |
| `loadTeams`         | **404**                               | Fetches `teams/teams.csv`; real asset is `teams/teams_colors_logos.{ext}`                                                                                     |
| `loadSchedules`     | **404**                               | Fetches `schedules/sched_{season}.csv`; real asset is the single all-seasons `schedules/games.{ext}` (Lee Sharpe). Wrong since day one; mocks hid it.         |
| `loadPlayerStats`   | **404**                               | Fetches legacy `player_stats/player_stats_{season}.csv`; nflreadr 1.5.0 (2025-09-02) moved to `stats_player/stats_player_{week\|reg\|post\|regpost}_{season}` |
| `loadParticipation` | **404**                               | Fetches `participation/participation_{season}.csv`; real tag/asset is `pbp_participation/pbp_participation_{season}`                                          |

Season validation is dynamic and correctly accepts 2026.

## 3. Upstream changes we must absorb (2025-2026)

1. **Player stats rebuild (nflreadr 1.5.0):** one combined file per summary level from `nflfastR::calculate_stats()`. `stat_type` (offense/defense/kicking) is deprecated; `summary_level` (`week`/`reg`/`post`/`reg+post`) now selects the **file** (`regpost` in filenames). Client-side aggregation in `player-stats.ts` becomes obsolete. New release tag `stats_team` + `load_team_stats()` sibling.
2. **Participation:** NGS participation ended after 2022. FTN donates 2023+ under **CC-BY-SA 4.0 (attribution required)**, delivered only after each postseason — cap default season at current-1 (nflreadpy does).
3. **Players v2 (1.5.0):** rebuilt players table, removed/renamed columns — `PlayerRecord` needs a schema pass.
4. **Depth charts (1.5.0):** now date-level (incl. preseason), ESPN-sourced — `DepthChartRecord` needs a schema pass.
5. **qs removed (1.5.1):** never supported here; fine. Valid formats: parquet, rds, csv(.gz).
6. **Season flip (1.5.1):** `most_recent_season()` flips the **Wednesday** after Labor Day. Check `getCurrentSeason()` in `datetime.ts` matches.

## 4. Parity gap vs nflreadpy (the port target)

nflreadpy ships 24 loaders; nflreadts has 8 (4 broken). Missing 16:

| Loader              | Asset                                             | Complexity                   |
| ------------------- | ------------------------------------------------- | ---------------------------- |
| `loadTeamStats`     | `stats_team/stats_team_{level}_{season}`          | Low (mirror of player stats) |
| `loadRostersWeekly` | `weekly_rosters/roster_weekly_{season}`           | Low                          |
| `loadDraftPicks`    | `draft_picks/draft_picks` (single file)           | Low                          |
| `loadCombine`       | `combine/combine` (single file)                   | Low                          |
| `loadInjuries`      | `injuries/injuries_{season}`                      | Low                          |
| `loadContracts`     | `contracts/historical_contracts` (single file)    | Low                          |
| `loadSnapCounts`    | `snap_counts/snap_counts_{season}`                | Low                          |
| `loadNextgenStats`  | `nextgen_stats/ngs_{passing\|receiving\|rushing}` | Low-med (stat_type arg)      |
| `loadOfficials`     | `officials/officials` (single file)               | Low                          |
| `loadPfrAdvstats`   | `pfr_advstats/advstats_{week\|season}_{type}`     | Med (2 args)                 |
| `loadTrades`        | `trades/trades` (single file)                     | Low                          |
| `loadFtnCharting`   | `ftn_charting/ftn_charting_{season}`              | Low                          |
| `loadFfPlayerids`   | dynastyprocess repo                               | Low                          |
| `loadFfRankings`    | dynastyprocess repo (`draft`/`week`/`all`)        | Low                          |
| `loadFfOpportunity` | ffverse/ffopportunity releases                    | Med                          |

nflreadpy deliberately skips `load_espn_qbr` and nflreadr's dictionaries/cleaning utilities; we adopt the same scope for parity, then decide extras later. Utility parity worth adding: `getCurrentWeek()`, `mostRecentSeason()` (Wednesday flip), team-abbr/player-name mapping tables.

## 5. Work plan (v0.4.0)

- **A — Fix the four broken loaders** — **DONE 2026-07-02** (commit `7674085`): all URLs repointed, `loadSchedules` is load-once-filter-seasons, `loadPlayerStats` uses pre-aggregated per-level files (client-side aggregation deleted), participation availability cap added, `weekly_rosters` tag fixed, `buildTeamStatsUrl` added, `PlayerStatsRecord` regenerated to the live 115-column schema. 798 unit tests green; all 13 live loader/format combinations verified.
- **B — `@nflverse/types`** — **PREPPED 2026-07-02**: tests absorbed into the git repo (`nflverse-types`), lint/typecheck cover tests, tarball clean (9 files, 5.6 kB). **Publish blocked on npm OTP (owner action):** run `npm publish --access public` in `nflverse-types` and complete the browser auth. Then: make nflreadts depend on it (ROADMAP §8.0) and delete the orphaned `nflverse-types-package` folder.
- **C — Parity loaders** — **DONE 2026-07-02**: all 16 built (typed records derived from live data, mocked tests, shared internals). Full suite: 986 unit tests green; **25/25 live checks pass across every loader**. Format realities discovered and documented: contracts + NGS have no plain csv upstream (parquet defaults), ff_rankings formats are fixed per type, draft/combine files include the upcoming class (allowFuture).
- **D — Guardrails + polish** — **DONE 2026-07-02 (shipped as 0.5.0)**: `scripts/live-smoke.mjs` (25 checks, exits non-zero on failure) + `.github/workflows/live-canary.yml` (Mon/Thu schedule + manual). Polish completed: parquet is the default format for all nflverse-data loaders (matching nflreadpy; live HEAD-verified every parquet asset exists), season flip is now the Wednesday after Labor Day (`getSeasonFlipDate()` exported), `PlayerRecord` regenerated to the 39-column players-v2 schema, `DepthChartRecord` regenerated as a two-era union (legacy weekly 2001-2024, ESPN date-level 2025+ — the live release ships both schemas, split by season), and `@nflverse/types@0.1.1` is now the foundation dependency (core types + shared constants re-exported; ROADMAP §8.0). Owner deprecated the unscoped `nflreadts` package and deleted the orphaned `nflverse-types-package` folder.

## 6. Canonical URL reference

`https://github.com/nflverse/nflverse-data/releases/download/<tag>/<file>.<ext>` — 25 live tags as of 2026-07-02: combine, contracts, depth_charts, draft_picks, espn_data, ftn_charting, injuries, misc, nextgen_stats, officials, pbp, pbp_participation, pfr_advstats, player_stats (legacy), players, players_components, rosters, schedules, snap_counts, stats_player (current), stats_team (current), teams, test, trades, weekly_rosters. Each release carries `timestamp.json`.

Other bases: `https://github.com/dynastyprocess/data/raw/master/files/` (ff_playerids, ff_rankings), `https://github.com/ffverse/ffopportunity/releases/download/` (ff_opportunity).
